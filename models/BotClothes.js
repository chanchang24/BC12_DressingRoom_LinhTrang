import ChoseItem from "./ChoseItem.js";

export default class BotClothes extends ChoseItem {
    constructor(id, type, name, desc, imgShow, imgSrc) {
        super(id, type, name, desc, imgShow, imgSrc);
    }
}